# Sometimes my Remote IDE (sshing to Oscar through VSCode) doesn't work...
# To fix it, I need to edit inside the following doc:
vi ~/.ssh/known_hosts
# and remove (dd) all lines beginning with "Oscar2", then save and exit (wq!)